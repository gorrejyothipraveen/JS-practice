const start = 4;
const end = 12;
const range = 1000;
let  randomNumber = Math.floor(Math.random() * range);

while (randomNumber < start || randomNumber > end) {
  randomNumber = Math.floor(Math.random() * range);
}

console.log(randomNumber);


